/**
 * @type { BusEvents }
 */
export const BUS_EVENTS = {
    generator: {
        onClick: 'generator: onclick'
    },
    points: {
        added: 'point: added',
        substracted: 'point: substracted',
        burnAll: 'point: burn all'
    }
}