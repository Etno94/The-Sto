Activo:
Current) Revisar generator manager linea 1009, no es exactamente el load que nos interesa.

1) Ver como chequear frame a frame el status update de cada cell cargada en estado Discharge.

- (content / UI) implementar mecanica de Pulse Generator
    - chequear pulse cells con loaded = true
    - generar secuencia disponible (deben ir en orden)
    - si al menos la primer pulse cell esta loaded, el generador se puede usar
    - al usar, se descargan las pulse cells en estado loaded, desde la ultima a la primera, respetando disponibilidad
    - la descarga se actualiza en cada frame (cada 14ms) tomando la duracion de descarga desde Data
        - en generator manager se va actualizando el attr de cellLoad
        - tiene que verse reflejado en el feedback visual, UIController
    - Mientras el generador esta activo, descargandose y generando puntos, tanto el generador como las celulas estan .disabled
    (para evitar interaccion indeseada)
    - Una vez que la ultima celula disponible en loaded es descargada, todas las celulas pasan a estar .enabled
    - se puede dar feedback visual de que solo las celulas encadenadas seran usadas en los pulsos

Tareas:

- (CSS) desacoplar elementos de generadores del id, plasmar estilos a traves de clases

Nice to have:

- actualizar render de solid point (actualmente se muestra tier 2 como tier 1) (tier 2 se llama heavy point)
- usar manager-index en todos lados
- tipar todos los parametros y obj
- en los Type, actualizar todas las prop tipo Function por () =>
- creacion de puntos / generadores tiene hardcodeado la animacion con la que entran. Hacer dinamico desde donde se plantea que animacion usar
- [Gamer Experience] 
    actual: los puntos se borran en una sola animacion. 
    Deseable: analizar si lo movemos a animar 1 punto a la vez (inicial) y a traves de mejoras que sean todos los puntos a la vez.

Nice to Know:

- Como funcionan los parametros (y calculos) de transform:matrix()

Bugs:

-low:
    - (visual) los generadores, al estar dentro de un wrapper, aparecen abruptamente en el canvas


---------------
---------------

Playthrough:

- Storage lvls:

lvl 1: max 3
cost: 1 basic

lvl 2: max 4
cost: 3 basic

lvl 3: max 5
cost: 5 basic

lvl 4: max 6
cost: 1 solid

lvl 5: max 7
cost: 3 solid

lvl 6: max 8
cost: 5 solid

lvl 7: max 9
cost: 7 solid

lvl 8: max 10
cost: 9 solid

lvl 9: max 11
cost: 11 solid

lvl 10: max 12
cost: 13 solid OR something new